# Protocol source

The authoritative protocol definition is [`../../proto/game.proto`](../../proto/game.proto)
at the repository root.

Copy or symlink that file into this directory when configuring Godot Protobuf tooling;
do not create a divergent protocol definition here.
